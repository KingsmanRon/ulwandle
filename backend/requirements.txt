# Web framework
# fastapi >= 0.137 pulls starlette >= 1.3.1, which clears
# PYSEC-2026-161 / CVE-2026-48817 / CVE-2026-48818 / CVE-2026-54282 /
# CVE-2026-54283 (DoS + header-handling). Keep both pinned together.
fastapi==0.137.1
uvicorn[standard]==0.32.1
gunicorn==23.0.0
python-multipart==0.0.32

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Caching / pub-sub
redis==5.2.0

# LLM
anthropic==0.39.0

# Crypto / auth
pyjwt[crypto]==2.13.0
argon2-cffi==23.1.0
pynacl==1.6.2

# Config / validation
pydantic==2.9.2
pydantic-settings>=2.7,<3
email-validator==2.2.0
python-dotenv==1.2.2

# Rate limiting
slowapi==0.1.9

# Observability
prometheus-client==0.21.0
python-json-logger==2.0.7

# Standard
httpx==0.27.2
python-dateutil==2.9.0
pytz==2024.2

# Data ingestion (DWS dam-levels PDF parsing, Cape Town HTML scraping)
# pdfminer.six is a transitive dep of pdfplumber; pinning directly to
# the 20251230+ release clears CVE-2025-70559.
pdfplumber==0.11.10
pdfminer.six==20260107
beautifulsoup4==4.12.3

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0
